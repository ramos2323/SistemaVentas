@model SistemaVentas.Models.Categoria
@{ ViewBag.Title = "Nueva Categoría"; }

<div class="ph">
    <div><div class="ph-title"><i class="bi bi-tags"></i> Nueva Categoría</div></div>
    <a href="/Categorias" class="btn-s"><i class="bi bi-arrow-left"></i> Volver</a>
</div>
<div class="card-dark" style="max-width:480px">
    <div style="padding:1.5rem">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <style>
                .fi {
                    width: 100%;
                    background: var(--surface2);
                    border: 1px solid var(--border);
                    border-radius: 10px;
                    padding: .68rem 1rem;
                    color: var(--text);
                    font-family: var(--fb);
                    font-size: .9rem;
                    outline: none;
                    transition: border-color .2s;
                    margin-bottom: 1.2rem;
                }

                    .fi:focus {
                        border-color: var(--accent);
                        box-shadow: 0 0 0 3px rgba(79,142,247,.12);
                    }

                .fl {
                    display: block;
                    font-size: .75rem;
                    font-weight: 600;
                    color: var(--text3);
                    letter-spacing: .05em;
                    text-transform: uppercase;
                    margin-bottom: .42rem;
                }

                .fe {
                    color: #f87171;
                    font-size: .76rem;
                    display: block;
                    margin-top: -.9rem;
                    margin-bottom: .7rem;
                }
            </style>
            <label class="fl">Nombre</label>@Html.TextBoxFor(m => m.Nombre, new { @class = "fi", placeholder = "Nombre de la categoría" })@Html.ValidationMessageFor(m => m.Nombre, "", new { @class = "fe" })
            <label class="fl">Descripción</label>@Html.TextBoxFor(m => m.Descripcion, new { @class = "fi", placeholder = "Descripción opcional" })
            <div style="display:flex;gap:.5rem"><button type="submit" class="btn-p"><i class="bi bi-check-lg"></i> Guardar</button><a href="/Categorias" class="btn-s">Cancelar</a></div>
        }
    </div>
</div>