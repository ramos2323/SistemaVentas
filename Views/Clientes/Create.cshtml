@model SistemaVentas.Models.Cliente
@{ ViewBag.Title = "Nuevo Cliente"; }

<div class="ph">
    <div><div class="ph-title"><i class="bi bi-person-plus"></i> Nuevo Cliente</div></div>
    <a href="/Clientes" class="btn-s"><i class="bi bi-arrow-left"></i> Volver</a>
</div>
<div class="card-dark" style="max-width:520px">
    <div style="padding:1.5rem">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <style>
                .fi {
                    width: 100%;
                    background: var(--surface2);
                    border: 1px solid var(--border);
                    border-radius: 10px;
                    padding: .68rem 1rem;
                    color: var(--text);
                    font-family: var(--fb);
                    font-size: .9rem;
                    outline: none;
                    transition: border-color .2s;
                    margin-bottom: 1.2rem;
                }

                    .fi:focus {
                        border-color: var(--accent);
                        box-shadow: 0 0 0 3px rgba(79,142,247,.12);
                    }

                .fl {
                    display: block;
                    font-size: .75rem;
                    font-weight: 600;
                    color: var(--text3);
                    letter-spacing: .05em;
                    text-transform: uppercase;
                    margin-bottom: .42rem;
                }

                .fe {
                    color: #f87171;
                    font-size: .76rem;
                    display: block;
                    margin-top: -.9rem;
                    margin-bottom: .7rem;
                }
            </style>
            <label class="fl">Nombre</label>@Html.TextBoxFor(m => m.Nombre, new { @class = "fi", placeholder = "Nombre completo" })@Html.ValidationMessageFor(m => m.Nombre, "", new { @class = "fe" })
            <label class="fl">Email</label>@Html.TextBoxFor(m => m.Email, new { @class = "fi", placeholder = "correo@ejemplo.com" })@Html.ValidationMessageFor(m => m.Email, "", new { @class = "fe" })
            <label class="fl">Teléfono</label>@Html.TextBoxFor(m => m.Telefono, new { @class = "fi", placeholder = "7000-0000" })
            <label class="fl">Dirección</label>@Html.TextBoxFor(m => m.Direccion, new { @class = "fi", placeholder = "Dirección" })
            <div style="display:flex;gap:.5rem">
                <button type="submit" class="btn-p"><i class="bi bi-check-lg"></i> Guardar</button>
                <a href="/Clientes" class="btn-s">Cancelar</a>
            </div>
        }
    </div>
</div>